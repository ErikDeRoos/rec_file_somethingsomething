# DLL examples

The DLL manual uses shared example data from `docs/examples`.

## Current example
- `docs/examples/1_simple_singlefile/user.rec`

This example is intentionally small and demonstrates:
- `%rec: User`
- a leading comment line
- two records
- multiline `Notes` field values

It is also intended to be consumed directly by tests.
