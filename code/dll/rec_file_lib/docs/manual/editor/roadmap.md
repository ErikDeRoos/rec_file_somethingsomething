# Editor roadmap

## Near-term direction
- Add a separate desktop editor project in the same repository
- Build it against the DLL instead of duplicating logic
- Show its own build and release visibility in GitHub
- Document its behavior under `docs/manual/editor`

## First intended milestone
- Open a rec file
- Inspect record sets and records
- Use the same shared library that powers the COM and .NET use cases
